# Local working notes — never committed (plan-folder convention)
plan/

# Dependencies / build output (TS + Vite, decided in handoff)
node_modules/
dist/
.vite/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# OS junk
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor / tooling junk
.idea/
.vscode/
.claude/
*.swp
*~
